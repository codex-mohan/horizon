# ============================================================================
# Environment Configuration for Horizon Agent
# ============================================================================
# Copy this file to .env and fill in your API keys
#
# MODEL CONFIGURATION IS NOW IN: src/agent/model_settings.py
# Edit that file to change provider, model name, temperature, etc.
#
# API keys below are loaded from environment variables for security.
# Never commit real API keys to version control!
# ============================================================================

# =============================================================================
# Provider Selection
# =============================================================================

# LLM Provider: anthropic, openai, google, groq, ollama, openai-compatible
# Default is groq for fast inference
LLM_PROVIDER=groq

# =============================================================================
# API Keys (select based on your provider)
# =============================================================================

# Anthropic API Key (required for anthropic provider)
ANTHROPIC_API_KEY=

# OpenAI API Key (required for openai provider)
OPENAI_API_KEY=

# Google API Key (required for google/gemini provider)
GOOGLE_API_KEY=

# Groq API Key (required for groq provider) - Get one at https://console.groq.com
GROQ_API_KEY=

# OpenAI-compatible API Key (if required by your provider)
# OPENAI_COMPATIBLE_API_KEY=

# =============================================================================
# Optional: Custom Base URLs
# =============================================================================

# Ollama - no API key required, but base URL if not localhost
# OLLAMA_BASE_URL=http://localhost:11434

# OpenAI-compatible API (for LM Studio, LocalAI, etc.)
# OPENAI_COMPATIBLE_BASE_URL=http://localhost:1234/v1

# OpenAI base URL override (if needed)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic base URL override (if needed)
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# =============================================================================
# Application Settings
# =============================================================================

# Environment: development, production
ENVIRONMENT=development

# Server settings
HOST=0.0.0.0
PORT=8000

# Database (if needed)
# DATABASE_URL=postgresql://user:password@localhost:5432/horizon
# REDIS_URL=redis://localhost:6379

# Logging
LOG_LEVEL=INFO

# =============================================================================
# LangSmith Tracing (Optional)
# =============================================================================
# Get your API key from https://smith.langchain.com

# LangSmith API Key
# LANGSMITH_API_KEY=

# Enable LangSmith tracing (set to true to enable)
# LANGCHAIN_TRACING_V2=true

# LangSmith project name (optional, defaults to "default")
# LANGCHAIN_PROJECT=horizon-agent
